# tests

Cross-package contract and integration tests. Package-local unit tests live alongside their package; this directory holds tests that span package boundaries (e.g. reconciliation-invariant fixtures, action-signature/idempotency contract tests, evidence-manifest completeness checks) per ADR-010's T0/T1 gate definitions.
