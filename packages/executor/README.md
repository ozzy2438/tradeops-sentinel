# packages/executor

Playwright-based deterministic legacy executor behind the Action Gateway interface (ADR-011). Post-Sprint-1 — not part of Epics E1-E6. UiPath remains a post-MVP, quote-gated interface swap; this package must never be relabelled as UiPath output.
