"""TradeOps Sentinel — reference implementation package root.

This package is currently a placeholder that exists to prove the Epic E1
CI foundation (lint, type-check, unit test) runs against real, non-trivial
output rather than an empty repository. Domain code (contracts, generator,
reconciliation, evidence, executor) lands in later Sprint 1 epics under
``packages/`` once their gating ADRs and contracts are approved and merged.
"""

__version__ = "0.1.0"
